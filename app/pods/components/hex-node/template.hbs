<g transform={{hexTransform}} {{action "clickNode" model}}>
  <polygon
    points={{points}}
    transform={{rotation}}
    fill={{color}}
  >
  </polygon>
  <text>
    <tspan fill={{textColor}}>{{model.resources.hexon}}</tspan>
  </text>
</g>